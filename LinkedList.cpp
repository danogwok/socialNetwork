/*#include "LinkedList.h"

int main(){
  time_t testTime;
  struct tm* t = localtime(&testTime);
  cout << (t->tm_sec) << endl;
  LinkedList<int> LL;
  for(int i=0; i < 10000; i++){
    LL.insert(i/2,1);
  }

  cout << (t->tm_sec) << endl;
    }
}*/
